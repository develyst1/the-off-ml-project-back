{
  "name": "the-off-ml-project-back",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --compile --minify --outfile dist/the-off-ml-project-back",
    "db:ensure": "bun src/scripts/ensure-database.ts",
    "db:migrate": "bun src/scripts/migrate-database.ts",
    "db:reset-test": "bun src/scripts/reset-test-data.ts",
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/pg": "^8.20.0",
    "hono": "^4.6.14",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
